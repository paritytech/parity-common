[package]
name = "tests"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]

[dependencies]
ethereum-types = { path ="../ethereum-types", features = ["std", "heapsizeof"] }
serde_json = "1.0"

[features]
use_asm = ["ethereum-types/use_asm"]
